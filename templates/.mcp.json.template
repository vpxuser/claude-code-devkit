{
  "$schema": "https://code.claude.com/schemas/mcp.json",

  "_comment": "[项目名称] MCP 服务器配置。[简要说明用途]",

  "mcpServers": {
    "[server-name]": {
      "command": "[command]",
      "args": ["[arg1]", "[arg2]"],
      "env": {}
    }
  }
}
